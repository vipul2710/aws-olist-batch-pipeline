version: 2

models:
  - name: dim_product
    columns:
      - name: product_sk
        tests:
          - not_null
          - unique

      - name: product_id
        tests:
          - not_null
          - unique

      - name: product_category_name
        tests: []   # NULLs allowed

      - name: product_length_cm
        tests: []   # NULLs allowed

      - name: product_height_cm
        tests: []   # NULLs allowed

      - name: product_width_cm
        tests: []   # NULLs allowed
